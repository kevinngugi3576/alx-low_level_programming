#include "main.h"

/**
 * mul - multiple two integers
 * @X: The first number to multiply
 * @y: The second number to multiply with first number
 * Return: The result of multiplication between x program and y
 */

int mul(int x, int y)
{
	return (x * y);
}
