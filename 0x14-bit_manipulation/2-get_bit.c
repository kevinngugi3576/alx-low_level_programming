#include "main.h"
#include <stdlib.h>

/**
 * get_bit - gets the value of a bit ata agiven index
 * @n: The bit
 * @index: the index to get the value of index at the start of 0
 *
 * Return: if NULL -1 else the value of bit index
 */
int get_bit(unsigned long int n, unsigned int index)
{
	if (index >= (sizeof(unsigned long int) * 8))
		return (0);

	return (1);
}
