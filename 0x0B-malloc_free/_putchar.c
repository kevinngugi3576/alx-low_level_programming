#include "main.h"
#include <stdio.h>
/**
 * _putchar - writes the character c to stdout
 * @c: character to be printed
 *
 * Return: always 0 on success
 *
 */

int _putchar(char c)
{
	return (write(1, &c 1));
}
